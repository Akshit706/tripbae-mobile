# User Taste Profile
- Provides detailed, multi-category checklists/requirements documents (markdown) to guide implementation work — expects all checklist items to be addressed systematically rather than a narrow fix. Confidence: 0.8
- Requires pixel-perfect preservation of UI when optimizing performance — explicitly insists that no UI, UX, layout, transitions, animations, colors, spacing, or user flow be changed during performance work; only performance, scrolling smoothness, touch responsiveness, and rendering efficiency should be improved. Confidence: 0.9
- Cares about responsive design across device dimensions — proactively questions whether fixed pixel/rem values will break on different phone sizes and wants layouts to adapt proportionally. Confidence: 0.8
- Prefers being educated about multiple approaches/solutions and their trade-offs (e.g., svh vs dvh vs max() vs flex) rather than being given a single fix without explanation. Confidence: 0.7
- Prefers industry-standard, battle-tested approaches when a decision needs to be made — after hearing options, asks "what's mostly used in industry and works fine" rather than evaluating trade-offs independently. Confidence: 0.6
- Prefers the direct fix over cautious workarounds — when a tool/dependency upgrade would solve a bug, pushes back on conservative workaround-first approaches ("if upgrading vite solves it then do it why taking this approach") and expects the upgrade to be attempted directly; only restructures code after the upgrade is proven insufficient. Confidence: 0.6
- Security-conscious about credential/API key exposure; expects secrets like Supabase anon keys to not live in frontend code, and prefers the backend-to-frontend one-time upload URL pattern for file uploads. Confidence: 0.9
- Cautious about architecture correctness — notices when the frontend directly calls the database and flags it as a concern. Confidence: 0.8
- Cares deeply about smooth scrolling and proper layout — reports jitteriness, scroll not working on specific pages, and content being hidden behind footers with high scrutiny. Confidence: 0.8
- Generalizes bug fixes across the codebase — when a bug is found and fixed on one page (e.g., scroll locking, header overlap), proactively checks whether the same class of bug exists on other pages and reports it. Confidence: 0.7
- Prefers thorough research on dependencies and tooling before acting — asks for confirmation on correct commands, versions (e.g., JDK 17), and questions if there's a simpler approach. Confidence: 0.8
- Likes documentation in markdown files — wants plans, lists of changes, and analysis written to `.md` files as a reference. Confidence: 0.7
- Creates comprehensive markdown issue documents as handoff artifacts for other agents/tools — when a complex bug persists across multiple fix attempts, documents all root causes found, attempted fixes (and why they didn't work), untried approaches, reproduction steps, and expected behavior so another agent can pick up the task independently. Confidence: 0.8
- Prefers incremental, step-by-step implementation ("one by one code these up") using documentation as a reference during execution. Confidence: 0.6
- Android-focused for mobile development — asks to build and run for Android specifically; redirects focus away from web builds to mobile APK building. Confidence: 0.7
- Interested in cross-platform mobile conversion via Capacitor (React web app → React Native / mobile app). Confidence: 0.6
- Reports bugs with clear, step-by-step reproduction sequences and describes expected vs. actual behavior precisely — including precise sensory/perceptual detail of visual artifacts (e.g., "on the new photo I see part of old photo for a sec, for a very small time") and precise localization of the symptom to a specific UI element. Confidence: 0.8
- Proactively proposes architectural solutions to structural bugs (e.g., "put content in a scrollable container below the fixed header") rather than asking for a fix — thinks in terms of layout patterns like flex containers and scrollable areas, not pixel hacks. Confidence: 0.6
- Wants system environment/tooling configuration (e.g., JDK paths, Java versions) proactively searched and persisted in taste files so future sessions don't miss them. Confidence: 0.8
- Expects the assistant to consult previous chat history for already-established environment/tooling facts (e.g., "Java 21 is on this system") before concluding a tool is missing — will push back with "check the previous chats" when the assistant declares something absent that was previously confirmed. Confidence: 0.7
- Java 21 is installed via Homebrew at `/opt/homebrew/opt/openjdk@21` — `JAVA_HOME` must be set to this path when building the Android APK. Confidence: 0.9
- Re-verifies fixes after they're applied and pushes back when issues persist — tests the deployed fix and reports back if the problem is only partially resolved, expecting thorough resolution rather than accepting partial or first-attempt fixes. Judges success by perceived on-screen smoothness (e.g., "it still has the slight reload") rather than by log state (e.g., `cached=true` in the logs) — expects the underlying cause (e.g., actual image bytes not yet downloaded) to be addressed, not just the cache flags. Confidence: 0.85
- When first-attempt fixes don't fully resolve an issue, explicitly expects the assistant to research the web and consult official documentation for known solutions and comprehensive approaches — rather than guessing more ad-hoc fixes. The user also proactively invites web research to validate their own proposed fixes when uncertain ("maybe i am wrong tho ? maybe search web its just swipe how hard can it be"). Confidence: 0.85
- Expects gesture-driven interactions (e.g., swipe cards) to have zero unnecessary React re-renders during the gesture — notices when child components like images or CTAs re-render during drag as visual artifacts (e.g., "the next image reloads and the create with lumi also re renders"), and expects the card to self-manage its DOM via refs rather than causing parent re-render cascades. Confidence: 0.8
- After making code changes, always build the Android APK (not just the web bundle). The exact build command is: `npx cap sync && cd android && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ANDROID_HOME=$HOME/Library/Android/sdk ./gradlew assembleDebug`. Confidence: 1.0
- Expects GPU-accelerated scrolling via CSS compositing layers on scrollable containers — uses `transform: translateZ(0)`, `will-change: scroll-position`, and `-webkit-overflow-scrolling: touch` on scrollable divs, page shells, bottom nav bars, and the body element for smoother rendering in Capacitor/Android WebView. Confidence: 0.8
- Before modifying existing features (e.g., bottom tabs, navigation), expects the assistant to cross-reference the local code against the GitHub repository version to verify whether the code is current or outdated — asks "check it please" to confirm state before making changes. Confidence: 0.7
- Proactively notices and questions UI/version discrepancies between environments (local/Android app, GitHub source, and deployed web) — has a mental model that the web build should match GitHub, and asks for root-cause explanations when they differ (e.g., stale build vs. code divergence). Confidence: 0.7
- Expects visual consistency across related UI elements in the same context — e.g., header logo dimensions should match bottom nav icon dimensions for a cohesive look; flags when elements are disproportionally sized relative to peers. Confidence: 0.6
- Communicates extremely tersely when re-running an established task — sends a single word or short phrase ("again", "re build the app", "continue") to re-trigger the full rebuild, expecting the assistant to recall and execute the whole prior workflow (web bundle build → Capacitor sync → Gradle APK build) without restating requirements or asking for clarification. Confidence: 0.85
- When re-running an established build ("rebuild the app"), explicitly instructs the assistant to first check the previous conversation and project docs (e.g., CAPACITOR_MIGRATION.md, MOBILE_FIXES_PLAN.md) to reconstruct how apps were previously built — treats prior sessions and markdown plans as the source of truth for the build workflow rather than expecting the assistant to start fresh or re-derive it. The user explicitly corrects the assistant when it looks in the wrong place: the build process lives in previous Command Code chat conversations, NOT in git log ("no in git log but in previous chat conversation of command code related to this project"). Confidence: 0.8
- "App logo" for this project means the native Android launcher icon — expects brand assets (e.g., a provided PNG) to be wired into the launcher icon across all densities including round and adaptive (Android 8+) variants, with the adaptive background matching the app's theme color, then a full APK rebuild. Confidence: 0.6
- When given a new brand logo, expects it to replace ALL remaining default Capacitor branding, not just the launcher icon — includes the splash screen (`@drawable/splash`), the web favicon, and in-app logo assets (e.g., `bgless.png`); pushes back tersely ("bro i still the capacitor icon") when any default branding is still visible after a rebuild, expecting the assistant to hunt down every remaining occurrence and rebuild again. Confidence: 0.8
- Inspects the rendered launcher icon at native size and expects the logo to display fully — flags when the logo is "cutting out" / clipped by the Android adaptive icon mask, expecting a padded version of the logo (safe-zone margins inside the icon canvas) so the mark sits fully inside the frame rather than edge-to-edge. Confidence: 0.8
- The product/app display name is "Trip Bae" (two words) — expects the launcher/app name to read "Trip Bae" (not TravelBae/travelbae) and uses "tripbae" as a shorthand for the brand mark. Confidence: 0.9
- Prefers debugging the mobile (Capacitor) app on the laptop by running the Vite dev server (`npm run dev` at http://localhost:5173/) and opening it in a regular Chrome tab — treats the browser-rendered app as a stand-in for the WebView since it loads the same React code and gives full DevTools, rather than debugging on an emulator/device first. Confidence: 0.6
- The shell environment on this machine has `NODE_ENV=production` set (inherited from the launching process, not from shell configs), which makes Vite's dev server treat dev mode as production — `@vitejs/plugin-react` then skips the React Fast Refresh preamble, producing `$RefreshSig$ is not defined` and a blank page at localhost. Dev server runs must override/clear NODE_ENV (e.g., `NODE_ENV=development npm run dev` or `env -u NODE_ENV npm run dev`). Confidence: 0.8
- Reports dev errors by pasting the raw console error with file:line and a one-line symptom ("Uncaught ReferenceError: $RefreshSig$ is not defined at TravelBae.jsx:31:34 the page is empty") — expects the assistant to root-cause from the error text rather than asking for more context. Also pastes full console log output from instrumented builds, including minified errors like "e is not a function", expecting the assistant to trace the cause (e.g., bundler/minification issues) from that output alone. Confidence: 0.95
- Uses the browser DevTools Network tab as the primary evidence for backend/debugging questions — reports "network tab is empty when I click" to prove a button fires no requests, and expects the assistant to trace the full click-to-fetch path (including in-flight dedup/cached promises) to explain why no request appears rather than doubting the observation. Confidence: 0.6
- During debugging, reports live observations of what IS working in the running app (e.g., "the frontend is authenticating me, the local taste endpoint works") as authoritative evidence to eliminate hypotheses — expects the assistant to accept it and narrow the search to the remaining non-working path rather than re-testing or re-litigating already-verified parts. Confidence: 0.5
- Questions whether UI buttons are genuinely wired to real functionality ("is it a fake button?") when an interaction produces no observable effect — expects interactive elements to be genuinely functional, and treats absence of a network request on click as a bug to root-cause, not a UI non-issue. Confidence: 0.6
- When the UI shows no data, prefers layer-isolated debugging: pastes a raw JWT auth token directly into the chat and expects the assistant to verify the backend first with direct curl/API calls against the endpoints (auth, trips, AI endpoints) to confirm the backend is healthy, and only then root-cause the frontend (e.g., token not in localStorage) — treats "is the backend responding?" as the first question to answer before touching frontend code. Confidence: 0.6
- Explicitly instructs NOT to check git history/status unless explicitly asked — for this project, git commands (git diff, git log, git status) should be skipped during debugging and implementation unless the user requests them; this supersedes any earlier expectation that git diffs be consulted as a debugging step. Confidence: 0.9
- Prefers simple console.log instrumentation added directly to the code as a debugging aid — explicitly asks for lightweight logs at UI/network boundaries (e.g., log on "Try Again" click, log when the fetch fires, log success/failure with the error message) so a reproduced bug reveals whether the endpoint is being hit, rather than heavier debugging tooling. Also asks for timing logs at key lifecycle points (pointer down/up, animation start, React commit, card render/mount) when diagnosing performance issues like swipe jitter, expecting the logs to be quick to add and easy to read. Confidence: 0.9
- Before debugging a UI flow, wants the assistant to state explicitly which API endpoint each screen/button is supposed to hit (e.g., which endpoint the explore page calls and under which error screen) so the expected data flow is clear up front. Confidence: 0.7
- Reports UI/layout bugs by pasting the rendered HTML/DOM of the offending elements straight from the running app (including all inline styles), expecting the assistant to diagnose the layout problem from that markup rather than asking for screenshots or more context. Confidence: 0.6
- Describes desired behavior in loose, informal terms and explicitly invites clarifying questions when intent is unclear ("i guess you understood if you have doubt ask") — comfortable with the assistant inferring intent and confirming. Confidence: 0.5
- Expects fixed app chrome (topbar/header AND bottom nav bar) to be hidden whenever any full-screen modal/bottom-sheet opens (e.g., the Create Trip wizard, the club info/description) — modal content must never sit underneath fixed chrome; flags partial overlap ("covered a little bit") as a bug. Explicitly endorsed hiding the header and bottom nav in the background while such windows are open, and generalized it to every overlay in the app, not just the reported one. Confidence: 0.85
- Prefers systemic, architecture-level fixes over per-overlay/per-page patches — endorsed hiding the header + bottom nav globally whenever any overlay opens (via a shared broadcast mechanism) over bumping z-index on each overlay one by one, reasoning that patch-per-case "would continue covering them". Confidence: 0.8
- When a previously-fixed bug (e.g., swipe jitter) resurfaces, expects the assistant to consult the project's markdown issue doc as the canonical record of root causes/fixes first, and to audit whether recent session changes reintroduced it ("did we introduce any of it?") before re-debugging from scratch. Confidence: 0.7
- Prefers eager preloading of assets over lazy loading for interactive surfaces — proposed loading all swipe-deck photos up front ("load all the images and just then when i swipe just show the next dont reload or anything") so swiping never waits on an async fetch mid-gesture, and endorsed the Tinder-style "preload the whole deck" pattern as the fix for load-time flashes during swipes. Confidence: 0.7
- Answers the assistant's clarifying diagnostic questions by selecting one of the enumerated symptom options and adding a terse follow-up request (e.g., "Does the whole card shift/bounce? (card transform issue) and maybe add logs for more details") — expects the assistant to proceed autonomously with the next instrumentation/investigation step rather than asking more questions. Confidence: 0.6
- The project's canonical GitHub remote is `https://github.com/Akshit706/tripbae-mobile` (remote alias `tripbae-mobile`, branch `main`) — when told to "push everything to this repo", expects all local work (both already-committed and uncommitted changes) to be staged, committed, and pushed there autonomously, verifying remote/branch state first rather than asking for clarification. Confidence: 0.8
